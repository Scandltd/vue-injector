export enum METADATA {
    TYPE = 'inject:type',
    VALUE = 'inject:value',
    NAME = 'inject:name',
    SERVICE = 'inject:service'
}

export enum FACTORY_TYPES {
    useFactory = 'useFactory',
    useValue = 'useValue',
    default = 'default'
}
